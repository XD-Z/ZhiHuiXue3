<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rhkj.zhihuixue">
    <!-- 网络请求权限 -->
    <uses-permission android:name="android.permission.INTERNET" /> <!-- 读写权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.login_register.LogoutActivity"></activity>
        <activity android:name=".activity.ChangePhoneActivity" />
        <activity android:name=".activity.login_register.AmendPasswordActivity" />
        <activity android:name=".activity.login_register.RegisterActivity" />
        <activity android:name=".activity.login_register.ForgetPasswordActivity" />
        <activity android:name=".activity.mine.SettingActivity" />
        <activity android:name=".activity.mine.AddressActivity" />
        <activity android:name=".activity.mine.QuestionActivity" />
        <activity android:name=".activity.mine.MessageActivity" />
        <activity android:name=".activity.mine.ShareActivity" />
        <activity android:name=".activity.mine.ScheduleActivity" />
        <activity android:name=".activity.mine.WeakvideoActivity" />
        <activity android:name=".activity.mine.RenewalsActivity" />
        <activity android:name=".activity.mine.MyorderActivity" />
        <activity android:name=".activity.mine.MycourseActivity" />
        <!-- 今日头条适配 -->
        <meta-data
            android:name="design_width_in_dp"
            android:value="375" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="667" /> <!-- 适配华为（huawei）刘海屏 -->
        <meta-data
            android:name="android.notch_support"
            android:value="true" /> <!-- 适配小米（xiaomi）刘海屏 -->
        <meta-data
            android:name="notch.config"
            android:value="portrait|landscape" />

        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--登陆-->
        <activity android:name=".activity.login_register.LoginActivity" />
        <!-- 智能英语介绍/服务介绍 -->
        <activity android:name=".activity.FuwuIntroduceActivity" />
        <!-- 商品详情 -->
        <activity android:name=".activity.CommodityDetailsActivity" />
        <!--订单详情/取消订单-->
        <activity android:name=".activity.OrderCancelActivity"/>
        <!--直记直译-->
        <activity android:name=".activity.ScheduleStraightActivity"/>


    </application>

</manifest>